<ul class="media-list">
  <% novels.each do |novel| %>
    <div class="col-xs-3">
      <div class="panel panel-default">
        <div class="panel-body">
        <div class="text-center">
          <h5>
            <%= link_to novel.title, novel_path(novel.id) %>
          </h5>
          <div>
            <li>auther:<%= link_to novel.user.user_name, user_path(novel.user) %></li>
            <li>number of pages</li>:<%= novel.chapters.count %></li>
            <li>number of subscribers:<%= novel.subscribe_users.count %></li>
            <li>number of likes:<%= novel.like_users.count %></li>
          </div>
        </div>
        </div>
        <div class="text-center">
          <% if logged_in? %>
            <% if current_user == novel.user %>
            <% else %>
              <%= render 'friendships/subscribe_button', novel: novel %>
              <%= render 'friendships/like_button', novel: novel %>
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
  <%= paginate novels %>
</ul>